<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom"><channel><title>Cloud Provider Azure – Design Docs and KEPs</title><link>https://kubernetes-sigs.github.io/cloud-provider-azure/development/design-docs/</link><description>Recent content in Design Docs and KEPs on Cloud Provider Azure</description><generator>Hugo -- gohugo.io</generator><atom:link href="https://kubernetes-sigs.github.io/cloud-provider-azure/development/design-docs/index.xml" rel="self" type="application/rss+xml"/><item><title>Development: Design proposal: Support multiple Standard LoadBalancer in one cluster</title><link>https://kubernetes-sigs.github.io/cloud-provider-azure/development/design-docs/multiple-slb/</link><pubDate>Mon, 01 Jan 0001 00:00:00 +0000</pubDate><guid>https://kubernetes-sigs.github.io/cloud-provider-azure/development/design-docs/multiple-slb/</guid><description>
&lt;h2 id="scenarios">Scenarios&lt;/h2>
&lt;p>Currently, there are three ways to determine which basic LB should be used by a load balancer typed service: default mode, auto mode, and &lt;code>as1,as2&lt;/code> mode (for more information: &lt;a href="https://kubernetes-sigs.github.io/cloud-provider-azure/topics/loadbalancer/)">https://kubernetes-sigs.github.io/cloud-provider-azure/topics/loadbalancer/)&lt;/a>. However, the standard LB doesn't support the mode selection annotation. The reason is that every vm in the vnet could be added to the backend pool of the SLB, including vms from different node pools (VMAS/VMSS). Hence, for SLB the selection is not needed.&lt;/p>
&lt;p>However, there are scenarios that we need multiple SLBs in the cluster. For example, users could set different outbound IPs for different VMAS or VMSS. In addition, more services are supported with multiple SLBs because there is a 4Mb restriction in an ARM request body.&lt;/p>
&lt;p>Basically, we would like to support the mode selection annotation (&lt;code>service.beta.kubernetes.io/azure-load-balancer-mode&lt;/code>) with SLB, similar to what it is with basic LB.&lt;/p>
&lt;h2 id="user-experience">User experience&lt;/h2>
&lt;p>Currently, only one SLB is allowed per cluster. If users set the mode selection annotation when using the SLB, they would get an error. After this feature implemented, the users could choose if they want a single SLB (current behavior), or they want multiple SLBs, with one SLB binding to one VMAS/VMSS. If the annotation is not set, everything is unchanged: every agent node in the cluster would join the backend pool of the SLB and only one SLB is allowed. If the annotation is set to &lt;code>__auto__&lt;/code> or &lt;code>nodepool1Name,nodepool2Name&lt;/code>, there would be a 1: 1 mapping between the SLBs and the VMAS/VMSS. Specifically, for &lt;code>__auto__&lt;/code> mode, the SLB with the minimum load balancing rules would be selected to serve the newly created service; for&lt;code>nodepool1Name,nodepool2Name&lt;/code> mode, the SLB of the given VMAS/VMSS with the minimum load balancing rules would be selected. In this way, users could set different outbound rules for different VMAS/VMSS. If there is no SLB of the given VMAS/VMSS, a new SLB would be created.&lt;/p>
&lt;h2 id="implementation">Implementation&lt;/h2>
&lt;p>There are three selection mode of the SLB: &lt;code>default&lt;/code>, &lt;code>__auto__&lt;/code> and &lt;code>nodepool1,nodepool2&lt;/code>. If the annotation &lt;code>service.beta.kubernetes.io/azure-load-balancer-mode&lt;/code> is not set for the SLB cluster, the behavior would be the same as it is now. If it is set to &lt;code>__auto__&lt;/code>, new services should choose the SLB with minimum rules. If it is set to &lt;code>nodepool1,nodepool2&lt;/code>, new services should choose the SLB with minimum rules of the given VMAS or VMSS.&lt;/p>
&lt;p>The following are the implementations in detail.&lt;/p>
&lt;ol>
&lt;li>
&lt;p>Introduce a new cloud provider config &lt;code>enableMultipleStandardLoadBalancers&lt;/code>, default to &lt;code>false&lt;/code>. When &lt;code>enableMultipleStandardLoadBalancers=false&lt;/code>, the mode selection annotation &lt;code>service.beta.kubernetes.io/azure-load-balancer-mode&lt;/code> would be ignored when using the SLB.&lt;/p>
&lt;/li>
&lt;li>
&lt;p>When &lt;code>enableMultipleStandardLoadBalancers=true&lt;/code>:&lt;/p>
&lt;ul>
&lt;li>
&lt;p>If the annotation &lt;code>service.beta.kubernetes.io/azure-load-balancer-mode&lt;/code> is not set, the SLB of the primary VMAS/VMSS would be selected for the services.&lt;/p>
&lt;/li>
&lt;li>
&lt;p>If &lt;code>service.beta.kubernetes.io/azure-load-balancer-mode = __auto__&lt;/code>, the SLB with minimum rules would be selected for the services.&lt;/p>
&lt;/li>
&lt;li>
&lt;p>If &lt;code>service.beta.kubernetes.io/azure-load-balancer-mode = nodepool1,nodepool2&lt;/code>, the SLB of the given VMAS/VMSS with minimum rules would be selected for the services. If there is no corresponding SLB, the cloud provider would start to create one with the naming format &lt;code>nodepoolName&lt;/code> or &lt;code>nodepoolName-internal&lt;/code>.&lt;/p>
&lt;/li>
&lt;/ul>
&lt;/li>
&lt;/ol>
&lt;p>The behaviors of other load balancing resources, e.g., load balancing rules, health probes, frontend IP configurations, etc., would keep unchanged.&lt;/p></description></item></channel></rss>